export default class Task {
  constructor(id, title, desc, checked) {
    this.id = id
    this.title = title
    this.desc = desc
    this.checked = checked
  }

  getAll() {

  }

  save() {
    
  }
}